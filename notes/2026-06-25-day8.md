# Day 8 — June 25, 2026

## Context

Tiago reviewed the experiment notes and raised an important concern:

The current train/validation split of 3070/100 may be too unbalanced. A validation set of 100 examples may produce noisy or artificially optimistic WER.

## Experiment 026 Plan

Purpose:

Re-run the Exp020 champion configuration using a full available validation set.

This creates a more trustworthy validation baseline before continuing optimization.

Reference configuration:

Exp020 champion

- Whisper Large-v3
- LoRA
- r = 16
- alpha = 32
- target_modules:
  - q_proj
  - k_proj
  - v_proj
  - out_proj
  - fc1
  - fc2
- learning_rate = 2e-5
- max_steps = 1000
- seed = 42

Split change:

- Train samples: 3070 → 2570
- Validation samples: 100 → 500

Only variable changed from Exp020:

- Train/validation split

Important note:

Do not compare Exp026 WER directly against Exp020 WER because the validation set changed.

Question:

Does the champion configuration remain strong under a larger, more representative validation set?

Success criterion:

Stable training and credible validation WER on 211 examples.


## Exp026 Results

Experiment Name

Exp026 — Whisper Large-v3 LoRA full available validation baseline

Configuration

Same as Exp020 champion except:

- Train samples: 3070 → 2570
- Validation samples: 100 → 211
- Validation set: full available FLEURS Swahili validation split

Results

- Final WER: 0.2318
- Final eval loss: 0.4474
- Train runtime: approximately 3h 7m
- Epochs completed: 1.556

Conclusion

Exp026 completed successfully.

The Exp020 champion configuration remained strong under the larger official validation split. Because Exp026 uses the full available validation split, it becomes the new trustworthy evaluation baseline for future experiments.

Important note:

Exp026 should not be compared directly against Exp020 as a pure WER improvement/loss because the validation set changed.

Decision

Use Exp026 as the new baseline for the next controlled experiment.

## Experiment 027 Plan

Purpose:

Test Tiago's suggestion to explicitly set the PEFT LoRA task type to SEQ_2_SEQ_LM.

Reference baseline:

Exp026

Only variable changed from Exp026:

- Add task_type=TaskType.SEQ_2_SEQ_LM to LoraConfig

Configuration:

- Model: Whisper Large-v3
- Method: LoRA
- Dataset: FLEURS Swahili
- Train samples: 2570
- Validation samples: 211
- Max steps: 1000
- Learning rate: 2e-5
- Seed: 42
- LoRA rank: 16
- LoRA alpha: 32
- LoRA dropout: 0.05
- Target modules:
  - q_proj
  - k_proj
  - v_proj
  - out_proj
  - fc1
  - fc2
- task_type: SEQ_2_SEQ_LM

Question:

Does explicitly setting the LoRA task type improve or stabilize Whisper seq2seq fine-tuning?

Success criterion:

WER < 0.2318 on the 211-example validation split.


## Exp027 Results

Experiment Name

Exp027 — Whisper Large-v3 LoRA task_type SEQ_2_SEQ_LM

Result

FAILED before training.

Error:

got multiple values for keyword argument 'input_ids'

Conclusion

task_type=TaskType.SEQ_2_SEQ_LM appears incompatible with the current PEFT + Whisper training script. This was not a GPU issue.

Decision

Do not use task_type=SEQ_2_SEQ_LM for this pipeline. Return to the Exp026 baseline configuration.


## Experiment 028 Plan

Purpose:

Determine whether automatically selecting the best validation checkpoint improves the final model.

Background:

Experiment 025 achieved the lowest observed WER (0.2257), but the final checkpoint degraded to 0.2371.

This suggests the best model may occur before the final training step.

Reference baseline:

Exp026

Configuration:

- Model: Whisper Large-v3
- Method: LoRA
- Train samples: 2570
- Validation samples: 211
- r = 16
- alpha = 32
- target_modules:
  - q_proj
  - k_proj
  - v_proj
  - out_proj
  - fc1
  - fc2
- learning_rate = 2e-5
- max_steps = 1000

Only variable changed:

Training now saves and restores the best validation checkpoint.

New Trainer settings:

- save_strategy = "steps"
- save_steps = 100
- evaluation_strategy = "steps"
- eval_steps = 100
- load_best_model_at_end = True
- metric_for_best_model = "wer"
- greater_is_better = False

Question:

Can automatic checkpoint selection outperform simply using the final checkpoint?

Success criterion:

Best checkpoint WER < 0.2318


## Experiment 028 Plan — Revised

Purpose:

Test whether an explicit cosine learning-rate scheduler improves over the Exp026 baseline.

Background:

The previous script did not explicitly define lr_scheduler_type. Best-checkpoint saving is now considered infrastructure and will remain enabled for all future experiments.

Reference baseline:

Exp026

Configuration:

Same as Exp026 except:

- lr_scheduler_type = "cosine"

Standard infrastructure now enabled:

- eval_steps = 100
- save_steps = 100
- load_best_model_at_end = True
- metric_for_best_model = "wer"
- greater_is_better = False

Question:

Does cosine learning-rate decay improve WER compared with the current default scheduler behavior?

Success criterion:

WER < 0.2318

### Exp028 Complete

Objective:
Evaluate cosine learning-rate scheduling while holding all other parameters constant.

Result:
- Validation WER: 0.2505

Outcome:
Cosine scheduling produced worse validation performance than the linear scheduler used in Exp026 (0.2318). This represents approximately an 8% relative increase in WER.

Decision:
Reject cosine scheduling for the current training configuration and return to the linear scheduler for future experiments.

Current Best Model:
Exp026 — Validation WER: 0.2318