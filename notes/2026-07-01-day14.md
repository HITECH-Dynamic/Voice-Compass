# Day 14 — 2026-07-01

## Focus
- Exp043 Multilingual Baseline

## Progress
- Successfully advanced beyond dataset preparation into the Whisper training pipeline.
- Fixed multiple infrastructure issues including Trainer API compatibility, dataset wrapper integration, Hugging Face authentication, and manifest generation.
- Confirmed the end-to-end training pipeline now reaches model execution.

## Key Findings
- Current multilingual sampling strategy is highly download-inefficient because selected samples are distributed across many large remote audio archives.
- Training spends most of its time downloading audio rather than utilizing the GPU.
- WER computation has not yet been integrated, so the experiment cannot yet be evaluated using the competition metric.

## Decisions
- Revise Exp043 before continuing training.
- Modify dataset construction to preferentially sample from fewer audio containers/shards.
- Add WER computation during evaluation.
- Save the best checkpoint based on lowest WER instead of lowest evaluation loss.

## Next Session
1. Implement download-efficient sampling.
2. Add compute_metrics() with WER.
3. Log WER to Weights & Biases.
4. Resume Exp043 multilingual baseline training.


## Exp043 Runtime Finding

Exp043 reached the training loop, but direct Swahili `.tar.xz` audio resolution caused severe slowdown inside Python `tarfile/lzma` seeking. The run was stopped after roughly 15 minutes before completing a single training step.

### Decision
Temporarily remove Swahili and validate the training loop + WER on Kikuyu first. Swahili will require a pre-extracted or cached-audio strategy before efficient training.
