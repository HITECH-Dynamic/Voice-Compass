
---

## Experiment 036A — AfriVoices Dataset Profiling

### Objective

Build a reusable dataset profiler to inspect the complete AfriVoices competition corpus before creating a unified training manifest.

### Major Accomplishments

- Successfully authenticated and accessed all gated Hugging Face datasets.
- Implemented a six-language profiler supporting seven source repositories.
- Successfully profiled:
  - Swahili
  - Kikuyu
  - Luo (Dholuo)
  - Somali
  - Maasai
  - Kalenjin
- Generated:
  - afrivoices_profile.md
  - afrivoices_language_summary.csv
  - afrivoices_split_summary.csv
  - afrivoices_top_dialects.csv
  - visualization figures
- Produced a reusable profiling pipeline for future datasets.

### Dataset Statistics

- Total utterances: 1,081,999
- Total measured audio: 5,542.97 hours
- Languages: 6
- Speakers detected: 3,577

Language distribution:

| Language | Hours |
|----------|------:|
| Swahili | 3201.18 |
| Luo | 681.70 |
| Kalenjin | 496.26 |
| Maasai | 482.67 |
| Somali | 478.19 |
| Kikuyu | 202.97 |

### Key Findings

- The corpus is highly imbalanced, with Swahili contributing the majority of training hours.
- Kaggle evaluates languages equally despite the imbalance, making future language-balancing experiments potentially valuable.
- Scripted and unscripted speech distributions differ substantially between languages.
- Swahili speaker metadata appears incomplete in the current parser implementation and should be revisited later if speaker-level analyses become important.
- The profiler successfully validates that all competition datasets are accessible and ingestible.

### Outcome

Experiment 036A successfully completed.

The project is now ready to build unified training manifests in Exp036B before beginning the first full AfriVoices training run.


---

## Experiment 036B — Unified Manifest Generation Plan

### Objective

Build normalized AfriVoices manifest files for training, validation, and testing.

### Inputs

- AfriVoices profiling outputs from Exp036A
- Swahili manifests from DigitalUmuganda/Afrivoice_Swahili
- ANV-KE metadata, transcripts, and audio parquet shards
- Somali sources from both Anv-ke/Somali and DigitalUmuganda/Afrivoice

### Target Outputs

- data/afrivoices/manifests/manifest_train.parquet
- data/afrivoices/manifests/manifest_dev.parquet
- data/afrivoices/manifests/manifest_test.parquet

### Required Columns

- id
- language
- language_name
- source_repo
- split
- speech_type
- audio_path
- transcription
- duration_seconds
- speaker_id
- dialect
- metadata

### Success Criteria

- One unified manifest format across all six competition languages
- Training script can read manifest_train.parquet and manifest_dev.parquet
- No missing required transcription fields in train/dev
- Language codes match Kaggle requirements: swa, kik, luo, som, mas, kln


---

## Experiment 036B — Unified AfriVoices Manifest Generation

### Outcome

Completed successfully.

### Outputs

- Train manifest: 1,015,803 rows
- Dev manifest: 35,578 rows
- Test manifest: 30,618 rows
- Summary: reports/dataset_analysis/manifest_summary.csv

### Notes

- Added source-aware row-level `unique_id` values.
- Original source IDs still contain 19,654 duplicates, but `unique_id` now resolves this.
- 42,523 train/dev rows are missing transcriptions and should be filtered before training.
- Large parquet manifest files are generated artifacts and were not committed.


---

## Exp037 Architecture Review — First Real AfriVoices Training Run

### Purpose

Exp037 will be the first training run using the real AfriVoices competition data rather than proxy data.

The purpose is not immediate leaderboard optimization. The purpose is to prove the full real-data training path:

manifest → audio resolution → preprocessing → training → validation → checkpoint

### Recommended Configuration

- Base model: openai/whisper-large-v3
- Training method: LoRA
- Hyperparameter baseline: Exp026 champion configuration
- Dataset: unified AfriVoices manifests from Exp036B
- Scope: filtered, capped multilingual subset

### Data Policy

Filter out:

- rows with missing or empty transcriptions
- rows with missing audio references
- rows with duration <= 0
- rows longer than 30 seconds

Use a balanced subset:

- up to 10,000 train rows per language
- up to 1,000 dev rows per language

This creates approximately:

- 60,000 training examples
- 6,000 validation examples

### Rationale

The full manifest contains over one million rows. Exp037 should validate the new training pipeline before spending significant GPU time.

A balanced subset prevents Swahili from dominating the first real-data run and aligns better with Kaggle's unweighted average WER across six languages.

### Required Engineering

Exp037 needs a real audio resolver:

- ANV-KE audio is stored as bytes inside parquet shards.
- Swahili audio is referenced through Swahili manifest/tar-style repository paths.
- The training dataset must resolve audio into arrays that Whisper can process.

### Evaluation Requirements

Exp037 should report:

- overall WER
- overall CER
- per-language WER
- per-language CER
- training loss
- validation loss
- learning-rate curve
- runtime
- checkpoint size

### Success Criteria

Exp037 is successful if:

- the unified manifests load correctly
- bad rows are filtered
- all six languages are represented
- audio references resolve
- training starts and completes
- validation metrics are produced
- a checkpoint is saved


---

## Exp037 Next Implementation Step

Next file to build:

- scripts/test_afrivoices_audio_resolver.py

Purpose:

Validate that the unified manifest can resolve real audio for both source types:

- parquet_bytes for ANV-KE datasets
- swahili_tar_ref for Swahili

This must work before writing the full training script.


---

## Experiment 037 — AfriVoices Audio Resolver Test

### Outcome

Completed successfully.

### Objective

Validate that unified manifest rows can resolve to real audio for both AfriVoices storage formats.

### Results

Successfully resolved and decoded:

- ANV-KE `parquet_bytes` audio
- Swahili `swahili_tar_ref` audio

### Key Findings

- ANV-KE audio was correctly located inside Parquet shards and decoded to 16 kHz waveform.
- Swahili audio was correctly located inside tar.xz archives and decoded to 16 kHz waveform.
- The `librosa` SoundFile warning for WebM is acceptable because decoding succeeded using the fallback backend.
- The resolver currently works but is inefficient for ANV-KE because it may scan many large Parquet shards before finding a file. This should be optimized before large-scale training.

### Outcome Summary

Exp037 proves the real competition audio can be resolved from the unified manifest.

The project is now ready for Exp038: building the PyTorch Dataset/DataLoader that uses this resolver.


---

## Experiment 038A — AfriVoices Dataset Class Prep

### Objective

Build a PyTorch-style dataset class that reads the unified AfriVoices manifests and returns real decoded audio examples.

### Scope

Exp038A will not use the Whisper processor yet.

It will only prove:

manifest row → audio resolver → waveform + transcript + metadata

### Target File

- scripts/test_afrivoices_dataset.py

### Success Criteria

- Loads manifest_train.parquet and/or manifest_dev.parquet
- Filters rows with missing transcripts
- Filters rows with missing audio references
- Samples across all six languages
- Resolves real audio using the Exp037 resolver logic
- Returns waveform, sample rate, transcription, language, duration, and unique_id
- Successfully prints at least one decoded example per language


---

## Experiment 038A — AfriVoices Dataset Class Test

### Outcome

Completed successfully.

### Results

- Created reusable dataset class: src/datasets/afrivoices_dataset.py
- Created test harness: scripts/test_afrivoices_dataset.py
- Loaded unified manifest.
- Filtered invalid rows.
- Returned decoded waveform, transcript, language, duration, and unique_id.
- Successfully tested both source formats:
  - Kikuyu from ANV Parquet audio
  - Swahili from tar.xz/WebM archive audio

### Notes

- The current ANV resolver works but can be slow because it scans large Parquet shards.
- Swahili WebM decoding uses librosa/audioread fallback successfully.
- Exp038A confirms the dataset layer works before Whisper processor integration.


---

## Experiment 038B — Whisper Processor Integration Prep

### Objective

Extend the AfriVoices dataset layer so decoded audio examples can be converted into Whisper-ready training examples.

### Scope

Exp038B will connect:

manifest → dataset class → audio resolver → waveform → Whisper processor → input_features + labels

### Target Files

- src/datasets/afrivoices_whisper_dataset.py
- scripts/test_afrivoices_whisper_dataset.py

### Success Criteria

- Loads the unified manifest.
- Uses the existing AfriVoicesDataset/audio resolver path.
- Loads openai/whisper-large-v3 processor.
- Converts decoded 16 kHz audio into Whisper input features.
- Tokenizes transcripts into label IDs.
- Prints tensor/array shapes for at least one Kikuyu example and one Swahili example.
- Does not start model training yet.

### Notes

This experiment prepares the final dataset interface needed for Exp039 training.


---

## Experiment 038B — Whisper Processor Integration

### Outcome

Completed successfully.

### Results

Successfully connected the unified AfriVoices dataset pipeline to the Whisper processor.

Verified complete pipeline:

Manifest
→ Dataset
→ Audio Resolver
→ Decoded Waveform
→ Whisper Feature Extractor
→ Whisper Tokenizer

### Validation

Tested on:

- Swahili (Digital Umuganda tar.xz/WebM)
- Kikuyu (ANV Parquet audio)

Both languages successfully produced:

- decoded 16 kHz waveform
- Whisper input_features tensor
- tokenized label IDs

Observed feature shapes:

- input_features: (128, 3000)
- labels:
  - Swahili: 87 tokens
  - Kikuyu: 67 tokens

### Notes

The reusable training dataset infrastructure is now complete.

Remaining work before model training focuses primarily on batching, collators, trainer configuration, evaluation, and optimization rather than additional data engineering.


---

## Experiment 039 — First End-to-End Whisper Training Prep

### Objective

Run the first end-to-end AfriVoices training experiment using the completed dataset pipeline.

### Scope

Exp039 will connect:

Unified Manifest
→ AfriVoicesWhisperDataset
→ Data Collator
→ WhisperForConditionalGeneration
→ Trainer
→ Validation Metrics
→ Saved Checkpoint

### Starting Strategy

Use a small balanced smoke-training subset first, not the full corpus.

Recommended first training subset:

- Languages: Kikuyu + Swahili
- Train examples: 10 per language
- Dev examples: 5 per language
- Max duration: 30 seconds

### Base Model

- openai/whisper-large-v3

### Training Method

- LoRA, using the strongest known configuration from the earlier optimization phase as the reference point.

### Success Criteria

- Model loads successfully.
- Dataset loads successfully.
- Data collator pads input features and labels correctly.
- Training starts without shape errors.
- At least one training step completes.
- Evaluation runs on the dev subset.
- Checkpoint is saved.
- Metrics/logs are written.

### Notes

Exp039 is not intended to beat the leaderboard yet.

Its purpose is to prove the complete model-training loop on real AfriVoices data.


---

## Experiment 039 — First End-to-End Whisper Training

### Outcome

Completed successfully.

### Objective

Demonstrate the complete end-to-end training pipeline using real AfriVoices data.

### Pipeline Verified

Manifest
→ AfriVoicesDataset
→ Audio Resolver
→ Whisper Feature Extraction
→ Whisper Tokenization
→ LoRA
→ Whisper Large-v3
→ Hugging Face Trainer
→ Evaluation
→ Checkpoint Saving

### Configuration

Model:
- openai/whisper-large-v3

Adapter:
- LoRA
- Rank: 16
- Alpha: 32
- Dropout: 0.05

Training

- GPU: NVIDIA L4
- Max steps: 1
- Batch size: 1
- Learning rate: 1e-5

### Results

Train rows:
- 1

Validation rows:
- 1

Training Loss:
- 1.033

Validation Loss:
- 1.8156

Checkpoint successfully written to:

checkpoints/exp039_smoke/

### Notes

The complete Voice-Compass training stack has now been validated on real AfriVoices data.

Remaining work is focused on scaling and optimization rather than infrastructure.

Known follow-up items:

- Improve ANV audio shard lookup performance
- Replace librosa/audioread fallback with a faster decoder
- Add WER metric computation
- Increase dataset size
- Enable mixed-language smoke runs
- Begin real optimization experiments


---

## Experiment 040A — ANV Audio Index Prep

### Objective

Build a fast lookup index for ANV-KE Parquet audio files so the dataset loader no longer has to scan many large shards to find one audio clip.

### Problem

The current ANV resolver works, but it is inefficient.

For a single Kikuyu example, the resolver may scan dozens of large Parquet shards before finding the matching audio file.

### Goal

Create an index mapping:

audio_filename → source_repo → split → speech_type → parquet_file

This will allow direct lookup of the correct Parquet shard during training.

### Target File

- scripts/build_anv_audio_index.py

### Expected Output

- data/afrivoices/indexes/anv_audio_index.parquet
- reports/dataset_analysis/anv_audio_index_summary.csv

### Success Criteria

- Index includes all ANV languages:
  - Kikuyu
  - Luo / Dholuo
  - Kalenjin
  - Maasai
  - Somali
- Index maps each audio filename to the Parquet shard containing it.
- Index can be loaded quickly by AfriVoicesDataset.
- Future dataset resolution avoids scanning all candidate shards.

